/**
 * @license
 * Copyright 2025 AionUi (aionui.com)
 * SPDX-License-Identifier: Apache-2.0
 */

// The built-in theme cards show the app itself: the home page at 1280x800, light and dark, cut to the card's shape
// (400x224, twice the card). Retake both when the shell changes, so the cards never show an older app.
import lightThemeCover from '@renderer/assets/themes/light-theme.png';
import darkThemeCover from '@renderer/assets/themes/dark-theme.png';

export { lightThemeCover, darkThemeCover };
